class User {
  final String firstName;
  final String lastName;
  final String email;
  final String city;
  final String country;

  const User(this.firstName, this.lastName, this.email, this.city, this.country);
}